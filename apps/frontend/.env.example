# All VITE_* values are embedded in the client bundle and must be treated as public.
VITE_API_BASE_URL=http://localhost:4000
VITE_SUPABASE_URL=https://twkqsaupojldddclgpqj.supabase.co
# Supabase anon key only — never put service-role or backend secrets here.
VITE_SUPABASE_ANON_KEY=your-anon-key
# Union Rental (customer-facing listings app) — referral links point here, not Fast Rental
VITE_PUBLIC_SITE_URL=http://localhost:5174
